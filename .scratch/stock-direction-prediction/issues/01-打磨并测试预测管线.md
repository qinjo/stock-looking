# 01 — 打磨并测试预测管线到接缝处

**What to build:** 在预测管线的接缝处提交一套 pytest 测试，锁定 demo 的既有行为。具体覆盖：
- `build_label`：验证「次日/后天（T+1 / T+2）收盘价 > 当日收盘价记为涨」的标签定义正确，且序列尾部正确产生 NaN。
- `evaluate`：用已知的小样本输入，验证 accuracy / AUC / win_rate / confusion-matrix 单元格的计算正确。
- `walk_forward_eval`：喂一个构造好的特征表，验证输出形状与测试行数一致、概率落在 [0,1]、且严格按时间顺序（训练不含未来数据）。
- 整套测试跑在合成/夹具数据上，不需要网络，也不依赖 eastmoney。

**Blocked by:** None — 可立即开始。

**Status:** resolved

- [x] `build_label` 对 horizon=1 和 horizon=2 的标签定义测试通过
- [x] `evaluate` 的指标计算测试通过（accuracy / AUC / win_rate / confusion 单元格）
- [x] `walk_forward_eval` 时序预测 + 概率合法性测试通过
- [x] 测试在无网络环境下可运行

## Answer

已在 `tests/` 新建 pytest 套件（`test_features.py` / `test_model.py`），离线合成数据运行，12 个测试全部通过。测试暴露并修复了一个真实 bug：`build_label` 原本用 `.astype(int)` 把序列尾部「无未来数据」的行坍缩成 `0`，导致 walk-forward 无法正确地丢弃它们；现改为保留 `NaN` 尾部（`float`），测试覆盖 horizon=1/2 标签定义、指标计算、时序预测与概率合法性。
