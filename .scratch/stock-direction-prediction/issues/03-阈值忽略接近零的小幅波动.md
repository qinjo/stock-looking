# 03 — 阈值：忽略接近零的小幅波动

**What to build:** 引入可配置的阈值，当「次日相对今日的涨跌幅度」落在 ±阈值 之内时，将该样本视为「横盘/中性」——既不当作涨也不当作跌，从预测中排除，从而过滤掉最容易误报的微幅噪声信号。阈值作为配置项（默认关闭或不设阈值），可通过 CLI / notebook 调整。

**Blocked by:** 01（打磨并测试预测管线）。

**Status:** resolved

- [x] 阈值作为可配置项（默认关闭 / 无阈值）
- [x] 落在 ±阈值 内的样本被标记为中性，不参与涨/跌二分类
- [x] CLI 与 notebook 可调整阈值
- [x] 阈值中性的样本有对应测试覆盖

## Answer

已实现。`features.py` 的 `build_label(df, horizon, threshold=0.0)` 新增 `threshold`：当 `threshold>0` 时，按「次日相对涨跌幅」把落在 `±threshold` 内的样本标记为中性（`NaN`，由 walk-forward 的 dropna 排除）；当 `threshold<=0` 时保持原二分类行为。`config.py` 新增 `THRESHOLD=0.0`，CLI 新增 `--threshold`，notebook 读取 `config.THRESHOLD`。新增测试 `test_threshold_neutralizes_tiny_moves` 与 `test_threshold_keeps_default_binary_behavior`。
