# 02 — 可解释性：输出特征重要性

**What to build:** 让每只股票的运行结果（CLI）和 notebook 都展示本次预测的主要驱动特征。使用 LightGBM 的 `feature_importances_`，报告特征重要性，让信号可解释而不是黑盒。用户能看到「这个涨/跌判断主要受哪些量价/技术指标影响」，从而抱着怀疑而非盲信的态度看待输出。

**Blocked by:** 01（打磨并测试预测管线）。

**Status:** resolved

- [x] 每只股票的评估输出包含特征重要性（按重要度排序）
- [x] CLI 输出能展示特征重要性
- [x] notebook 能展示特征重要性
- [x] 特征重要性基于训练得到的模型，且数值合法

## Answer

已实现。`model.py` 新增 `feature_importances(df, feature_cols, label_col, n=10)`（用 LightGBM 的 `feature_importances_`，按重要度降序返回前 `n` 个）。`stocklook/__main__.py` 的 CLI 会在指标表后打印每只股票前 5 个特征重要性；`notebooks/demo.ipynb` 新增 `imp_df` 展示各股票的特征重要性。新增测试 `TestFeatureImportances` 覆盖返回排序合法性。
